{
  "name": "vespa-parts-store-server",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "description": "Server lokal untuk konten (sparepart, kategori, service, logs) + upload gambar. Login & register tetap memakai MockAPI, tidak lewat server ini.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1"
  }
}
